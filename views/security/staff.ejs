<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Staff List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #0b0b0b;
            padding: 20px;
        }

        h1 {
            text-align: center;
            color: #f3ecec;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            background-color:lightblue;
            border: 1px solid #060505;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #0a0909;
        }

        th {
            background-color:burlywood;
            font-weight: bold;
            color: #333;
        }

        tr:hover {
            background-color: #e0e0e0;
        }
    </style>
</head>
<body>
    <h1>Staff List</h1>
    <table border="1">
        <tr>
            <th>Name</th>
            <th>Cabin No</th>
            <th>Phone No</th>
            <th>Email ID</th>
        </tr>
        <% staff.forEach(function(staffMember) { %>
            <tr>
                <td><%= staffMember.Name %></td>
                <td><%= staffMember.Cabin_no %></td>
                <td><%= staffMember.Phone_no %></td>
                <td><%= staffMember.Email_id %></td>
            </tr>
        <% }); %>
    </table>
</body>
</html>
